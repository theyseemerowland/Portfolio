<!DOCTYPE html>
<html>
<head>
    <style>
        /* THIS IS THE FRONTEND DESIGN (CSS) */
        body { 
            background-color: #121212; /* Changes the background to dark mode */
            color: #00d4ff;           /* Changes all text to a neon blue */
            font-family: 'Courier New', monospace; 
            padding: 50px;
        }
        .project-card {
            border: 1px solid #00d4ff;
            padding: 20px;
            margin-bottom: 10px;
            border-radius: 10px;
        }
    </style>
</head>
<body>

    <h1>Timothy Rowland: Developer Portfolio</h1>
</body>
</html>
    <?php
    /* THIS IS THE BACKEND LOGIC (PHP) */
    $conn = new mysqli('localhost', 'root', '', 'portfolio_db');
    $sql = "SELECT title, description FROM projects";
    $result = $conn->query($sql);

    while($row = $result->fetch_assoc()) {
        echo "<div class='project-card'>";
        echo "<h2>" . $row['Customer Service'] . "</h2>";
        echo "<p>" . $row['n'] . "</p>";
        echo "</div>";
    }
    ?>

echo "<h1>System Restoration Successful</h1>";
echo "<p>Your server is back online. You can now start adding your HTML and CSS back in slowly.</p>";

